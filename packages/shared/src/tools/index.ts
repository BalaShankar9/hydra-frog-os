/**
 * Shared Tools Rules
 * 
 * This directory contains shared business logic for tools.
 * Each tool has its rules scaffolded using: pnpm scaffold:tool --spec <toolSpecId>
 * 
 * Structure per tool:
 *   <toolKey>/
 *     rules.ts  - Validation, calculations, and business rules
 *     index.ts  - Barrel exports
 * 
 * Usage from API:
 *   import { validateMyToolInput } from '@hydra-frog/shared/tools/my-tool/rules';
 */

// Export tool rules here after scaffolding
// Example:
// export * from './my-tool';
