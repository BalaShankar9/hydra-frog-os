{
  "name": "hydra-frog-os",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "dev:infra": "docker compose -f infra/docker-compose.yml up -d",
    "dev:api": "pnpm --filter api dev",
    "dev:dash": "pnpm --filter dashboard dev",
    "dev:crawler": "pnpm --filter crawler dev",
    "dev:all": "concurrently -n api,dash,crawler -c blue,green,yellow \"pnpm dev:api\" \"pnpm dev:dash\" \"pnpm dev:crawler\"",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "clean:all": "turbo run clean && rm -rf node_modules",
    "docker:up": "docker compose -f infra/docker-compose.yml up -d",
    "docker:down": "docker compose -f infra/docker-compose.yml down",
    "docker:logs": "docker compose -f infra/docker-compose.yml logs -f"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "concurrently": "^9.2.1",
    "eslint": "^8.57.0",
    "prettier": "^3.2.0",
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  }
}
